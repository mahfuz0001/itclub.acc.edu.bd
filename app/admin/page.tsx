import AdminDashboard from "@/components/admin/admin-dashboard";

export const metadata = {
  title: "Admin Dashboard | IT Club",
  description: "IT Club admin dashboard",
};

export default function AdminPage() {
  return <AdminDashboard />;
}
